{
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app.main:app"
        }
    ],
    "builds": [
        {
            "src": "/app/main:app",
            "use": "@vercel/python"
        }
    ]
}